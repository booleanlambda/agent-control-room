name: AAU Independent Expertise Verification (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated independent verification path
        run: |
          echo "Deprecated: AAU independent expertise verification now runs on Render using direct NVIDIA endpoints."
          echo "Authentication uses z-ai/glm-5.3 and final authorization is an AAU deterministic gate."
          echo "This workflow no longer calls OpenRouter."
