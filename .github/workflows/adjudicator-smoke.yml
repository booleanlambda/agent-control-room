name: Adjudicator Smoke Test (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated adjudicator path
        run: |
          echo "Deprecated: AAU expertise adjudication now runs on Render through direct NVIDIA endpoints."
          echo "This workflow no longer calls OpenRouter."
