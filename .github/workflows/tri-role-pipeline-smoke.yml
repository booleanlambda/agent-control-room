name: AAU Three-Role Pipeline Smoke Test (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated tri-role path
        run: |
          echo "Deprecated: AAU expertise verification now runs on Render using direct NVIDIA endpoints."
          echo "Task authority is deterministic AAU code; authentication uses z-ai/glm-5.3; adjudication uses a separate NVIDIA-hosted model when required."
          echo "This workflow no longer calls OpenRouter."
