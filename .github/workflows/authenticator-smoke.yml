name: Authenticator Smoke Test (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated verifier path
        run: |
          echo "Deprecated: AAU expertise authentication now runs on Render through direct NVIDIA NIM with z-ai/glm-5.3."
          echo "This workflow no longer calls OpenRouter."
