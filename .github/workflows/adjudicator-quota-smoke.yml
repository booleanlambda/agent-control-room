name: Adjudicator Quota Smoke (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated quota path
        run: |
          echo "Deprecated: AAU expertise verification no longer uses OpenRouter quotas."
          echo "Direct NVIDIA endpoint availability is validated by the Render verifier runtime."
